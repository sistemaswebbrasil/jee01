<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:h="http://java.sun.com/jsf/html" xmlns:f="http://java.sun.com/jsf/core"
    xmlns:p="http://primefaces.org/ui" xmlns:ui="http://java.sun.com/jsf/facelets">
<ui:composition template="../template/master-page.xhtml">

    <f:metadata>
        <f:viewParam name="id" value="#{${view.beanForm}.${entity.id.var}}">
        </f:viewParam>
        <f:viewAction action="#{${view.beanForm}.init()}" />
    </f:metadata>

    <ui:define name="title">
        <ui:fragment rendered="#{empty ${view.beanForm}.${entity.id.var}}">#{lb['${entity.var}.entityCreate']}
        </ui:fragment>
        <ui:fragment rendered="#{not empty ${view.beanForm}.${entity.id.var}}">#{lb['${entity.var}.entityEdit']}
        </ui:fragment>
    </ui:define>

    <ui:define name="title">#{lb['${entity.label.create}']}</ui:define>
    <ui:define name="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">

                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title">
                                #{msg['scaffold.entity.detail']}<small>#{scaffoldBean.selected.name}</small>
                            </h3>
                        </div>
                        <div class="card-body">
                            <h:form id="form">
                                <p:panelGrid columns="4" layout="flex"
                                    columnClasses="p-col-12 p-md-6 p-lg-3,p-col-12 p-md-6 p-lg-3,p-col-12 p-md-6 p-lg-3,p-col-12 p-md-6 p-lg-3"
                                    contentStyleClass="p-align-baseline ">
                                    ${partial.fields.begin}
                                    <p:outputLabel for="${entity.field}" value="#{lb['${entity.var}.${entity.field}']}"
                                        ${optional.render} />
                                    <p:inputText id="${entity.field}"
                                        value="#{${view.beanForm}.${entity.var}.${entity.field}}" ${optional.render} />
                                    ${optional.hidden}
                                    ${partial.fields.end}
                                </p:panelGrid>
                                <div class="card-footer">
                                    <div class="margin float-right">
                                        <div class="btn-group">
                                            <p:commandLink action="index.xhtml?faces-redirect=true" value="#{lb['${entity.label.list}']}"
                                                styleClass="btn btn-primary float-right">
                                                <span class="fas fa-table"></span>
                                            </p:commandLink>
                                        </div>
                                        <div class="btn-group">
                                            <p:commandLink value="#{msg.save} " action="#{${view.beanForm}.save}"
                                                update="@all" styleClass="btn btn-primary float-right">
                                                <span class="fas fa-edit"></span>
                                            </p:commandLink>
                                        </div>
                                    </div>
                                </div>
                            </h:form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </ui:define>
</ui:composition>
</html>