<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:h="http://java.sun.com/jsf/html" xmlns:f="http://java.sun.com/jsf/core"
	xmlns:p="http://primefaces.org/ui" xmlns:ui="http://java.sun.com/jsf/facelets">
<ui:composition template="../template/master-page.xhtml">

<f:metadata>
	<f:viewParam name="id" value="#{${view.beanForm}.${entity.id.var}}">
	</f:viewParam>
	<f:viewAction action="#{${view.beanForm}.init()}" />
</f:metadata>

<ui:define name="title">
	<ui:fragment rendered="#{empty ${view.beanForm}.${entity.id.var}}">#{lb['${entity.var}.entityCreate']}</ui:fragment>
	<ui:fragment rendered="#{not empty ${view.beanForm}.${entity.id.var}}">#{lb['${entity.var}.entityEdit']}</ui:fragment>
</ui:define>

<ui:define name="title">#{lb['${entity.label.create}']}</ui:define>
	<ui:define name="content">
		<h:form id="form">			
			<div class="p-grid">
				<div class="p-col p-col-align-start">
					<p:panel id="panel" header=" ">
						<p:panelGrid columns="4" layout="flex"
							columnClasses="p-col-12 p-md-6 p-lg-3,p-col-12 p-md-6 p-lg-3,p-col-12 p-md-6 p-lg-3,p-col-12 p-md-6 p-lg-3"
							contentStyleClass="p-align-baseline ">
							
							${partial.fields.begin}
							<p:outputLabel for="${entity.field}" value="#{lb['${entity.var}.${entity.field}']}" ${optional.render}/>
							<p:inputText id="${entity.field}" value="#{${view.beanForm}.${entity.var}.${entity.field}}" ${optional.render} />
							${optional.hidden}
							${partial.fields.end}
							
						</p:panelGrid>
					</p:panel>
				</div>
			</div>			
			<p:toolbar>
				<f:facet name="right">					
					<p:linkButton href="index.xhtml" value="#{lb['${entity.label.list}']}" icon="pi pi-search" style="margin-right:20px;" />
					<p:commandButton value="#{msg.save}" action="#{${view.beanForm}.save}" update="@form messages" icon="pi pi-save" 
					/>					
				</f:facet>
			</p:toolbar>			
		</h:form>
	</ui:define>
</ui:composition>
</html>